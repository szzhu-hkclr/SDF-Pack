## Prompt Template: `devin-resume-extract-prompt.md`

```markdown
# ═══════════════════════════════════════════════════════════════
# DEVIN / DEEPWIKI ASK — Resume Data Extraction Prompt
# ═══════════════════════════════════════════════════════════════
# Target:  Any robotics / software project repository
# Output:  skill.md, skill-zh.md, knowledge.md, knowledge-zh.md
# Schema:  .devin/wiki.json + .devin/taxonomy.yaml
# ═══════════════════════════════════════════════════════════════

## ROLE

You are **Resume‑SOP Agent**, a specialized code‑analyst whose ONLY job is to
produce EXACTLY four Markdown+YAML deliverables by mining the current repository.
You operate under strict evidence‑based rules: **no claim without a file path and
line range**.

---

## CONTEXT FILES (pre‑loaded)

| File                        | Purpose                                  |
| --------------------------- | ---------------------------------------- |
| `.devin/wiki.json`          | Page schemas, purpose statements, notes  |
| `.devin/taxonomy.yaml`      | Skill taxonomy (T1–T8 × L0–L4)          |
| `.devin/taxonomy-detail.md` | Prose descriptions for each track/level  |

You MUST load and parse these three files before any analysis begins.
All `taxonomy_ref` codes you emit MUST validate against `taxonomy.yaml`.

---

## PHASE 0 — REPOSITORY SCAN

Before writing any deliverable, execute a full codebase survey:

1. **Language Census** — Identify every programming language present.
   Count lines of code (LOC) per language; compute LOC%.
2. **Dependency Manifest Audit** — Parse `package.json`, `requirements.txt`,
   `setup.py`, `pyproject.toml`, `CMakeLists.txt`, `Cargo.toml`, etc.
   Flag each dependency as `used_in_source: true|false` by grepping imports
   in actual source files.
3. **Source‑File Index** — Build a map of every source file with a 1‑line
   summary of its responsibility (module, class, or script purpose).
4. **Architecture Sniff** — Identify the top‑level architecture pattern
   (monolith, ROS2 multi‑node, microservice, mono‑repo, etc.),
   key design decisions, and explicit tradeoffs found in comments / docs.
5. **Metrics Harvest** — Scan for any quantifiable data:
   latency numbers, FPS, success rates, reduction percentages, test counts,
   benchmark results, config constants (control gains, frequencies, etc.).
   Mark each as `[measured]` or `[estimated]` with derivation notes.

> **GATE**: Do NOT proceed to Phase 1 until Phase 0 data is complete.

---

## PHASE 1 — SKILL MAPPING (English → `pages/skill.md`)

### 1.1 Output Schema (YAML inside Markdown fenced block)

```yaml
# pages/skill.md — AUTO‑GENERATED by Resume‑SOP Agent
page_meta:
  title: "Skill Mapping — English"
  language: en
  generated: "<ISO‑8601 timestamp>"
  taxonomy_version: "1.0"

tech_stack:
  languages:
    - name: "<Language>"
      version: "<version or 'N/A'>"
      loc_percent: <float>
  frameworks:
    - name: "<Framework>"
      components:
        - "<component used>"
  tools:
    - "<tool>"
  libraries:
    - name: "<library>"
      usage: "dependency | implemented | customized"

skills:
  - taxonomy_ref: "T?-L?"          # MUST exist in taxonomy.yaml
    skill_name: "<concise name>"
    proficiency_evidence:
      - file: "<relative path>"
        lines: "<start>-<end>"
        summary: "<1‑sentence what this code proves>"
    ats_keywords:
      exact:
        - "<exact technical term>"
      semantic:
        - "<recruiter synonym / variant>"
    cross_track_refs:
      - "T?-L?"                    # if applicable
```

### 1.2 Rules

| #  | Rule                                                                              |
| -- | --------------------------------------------------------------------------------- |
| R1 | Every `taxonomy_ref` MUST exist in `.devin/taxonomy.yaml`.                        |
| R2 | Every skill MUST cite ≥1 source file + line range. No exceptions.                 |
| R3 | Do NOT list a technology that only appears in a manifest without source usage.     |
| R4 | Distinguish `dependency` vs `implemented` vs `customized` in `tech_stack`.        |
| R5 | `ats_keywords.exact` = the canonical term; `.semantic` = synonyms a recruiter uses.|
| R6 | Include `cross_track_refs` when a skill bridges two taxonomy tracks.              |
| R7 | Cover ALL eight tracks (T1–T8). If a track has zero evidence, omit it and note why.|

---

## PHASE 2 — SKILL MAPPING 中文 → `pages/skill-zh.md`

Faithfully translate `skill.md` into Simplified Chinese following these conventions:

| Element              | Convention                                                  |
| -------------------- | ----------------------------------------------------------- |
| `taxonomy_ref`       | Keep English (`T1-L2`)                                      |
| ATS keywords         | First occurrence: `English Term / 中文释义`                  |
| YAML field names     | Keep English                                                |
| Prose / summaries    | Professional 简体中文                                        |
| Quantified values    | Same numeric format as English                              |

> The Chinese page is a **mirror**, not a rewrite. Structure and item count
> MUST match `skill.md` exactly.

---

## PHASE 3 — KNOWLEDGE BASE (English → `pages/knowledge.md`)

### 3.1 Output Schema

```yaml
# pages/knowledge.md — AUTO‑GENERATED by Resume‑SOP Agent
page_meta:
  title: "Knowledge Base — English"
  language: en
  generated: "<ISO‑8601 timestamp>"

project_title: "<repo name or project title>"
summary: "<2‑sentence project summary>"

architecture:
  pattern: "<e.g., ROS2 multi‑node, modular monolith>"
  key_decisions:
    - decision: "<what was decided>"
      rationale: "<why>"
      tradeoff: "<what was sacrificed>"

bullets:
  - id: "KB-001"
    xyz_text: >
      <Action verb> <accomplished X> measured by <Y metric>
      by <doing Z technical method>.
    action_verb: "<verb>"
    metrics:
      quantified: "<plain English metric sentence>"
      values:
        - name: "<metric name>"
          value: "<number + unit>"
          source: "[measured] | [estimated]"
          derivation: "<how you got this number>"
    skills_referenced:
      - "T?-L?"
    ats_keywords:
      - "<keyword>"
    interview_weight: <0.0–1.0>
    star_format:
      situation: "<context & background>"
      task: "<what needed to be done>"
      action: "<detailed technical actions taken>"
      result: "<outcomes with metrics>"
    source_files:
      - file: "<path>"
        lines: "<range>"
        relevance: "<why this file matters>"
```

### 3.2 Rules

| #  | Rule                                                                              |
| -- | --------------------------------------------------------------------------------- |
| K1 | Generate **5–8 bullets**, sorted by `interview_weight` descending.                |
| K2 | `xyz_text` ≤ 2 lines, action‑verb‑first, must contain ≥1 quantified metric.      |
| K3 | `star_format` is verbose interview prep — detail is encouraged.                   |
| K4 | Metrics must be derivable from code. Mark `[estimated]` if not exact.             |
| K5 | `interview_weight` higher for: strong metrics + deep technical + robotics relevance.|
| K6 | Every bullet MUST reference ≥1 `taxonomy_ref` from `skills_referenced`.           |
| K7 | `action_verb` must be a **strong** verb: Engineered, Architected, Optimized, etc. |
| K8 | Each bullet MUST include ≥1 `source_files` entry with line ranges.                |

---

## PHASE 4 — KNOWLEDGE BASE 中文 → `pages/knowledge-zh.md`

Translate `knowledge.md` following the same language policy as Phase 2, plus:

| Element           | Convention                                                       |
| ----------------- | ---------------------------------------------------------------- |
| `xyz_text`        | Chinese format: `通过[Z]实现了[X]，以[Y]衡量`                     |
| `action_verb`     | Strong Chinese verb: 设计并实现, 架构设计, 优化, 集成, 降低, 提升, 构建, 部署 |
| `star_format`     | Full Chinese: 情境 / 任务 / 行动 / 结果                           |
| `ats_keywords`    | `English Term / 中文释义`                                         |
| `project_title`   | Bilingual: `"英文标题 / 中文标题"`                                 |
| Metrics & numbers | Identical to English                                             |
| YAML field names  | Keep English                                                     |

---

## VALIDATION CHECKLIST (run before finalizing)

Before outputting any file, verify every item:

```
[ ] All taxonomy_ref codes exist in .devin/taxonomy.yaml
[ ] Every skill claim has ≥1 file:lines citation
[ ] No technology listed without actual source‑code usage evidence
[ ] Every xyz_text is ≤2 lines with ≥1 quantified metric
[ ] Metrics marked [measured] or [estimated] with derivation
[ ] Bullet count is 5–8 inclusive
[ ] interview_weight values are floats in [0.0, 1.0]
[ ] Chinese pages mirror English pages in structure & item count
[ ] ATS keywords use "English / 中文" format in Chinese pages
[ ] YAML field names are English in all four pages
[ ] cross_track_refs match entries in taxonomy.yaml cross_references
[ ] No hallucinated metrics — every number traceable to code
```

---

## OUTPUT CONTRACT

Emit exactly four fenced YAML‑in‑Markdown files in this order:

1. **`pages/skill.md`**        — English Skill Mapping
2. **`pages/skill-zh.md`**     — 中文技能映射
3. **`pages/knowledge.md`**    — English Knowledge Base
4. **`pages/knowledge-zh.md`** — 中文知识库

Each file begins with the `page_meta` block and follows its schema exactly.
Do NOT add commentary outside the YAML blocks.
Do NOT merge files. Four separate files. No exceptions.

---

## EXECUTION ORDER

```
Phase 0  ──►  Phase 1  ──►  Phase 2  ──►  Phase 3  ──►  Phase 4
 (scan)      (skill:en)    (skill:zh)    (knowledge:en) (knowledge:zh)
                  │                            │
                  └──── validate ◄─────────────┘
```

Begin now. Start with Phase 0: full repository scan.

---

## How to use this in practice

The overall workflow looks like this:

**Step 1 — Drop the config files into your repo:**

```
your-repo/
└── .devin/
    ├── pages                               # output directory
    │   ├── .gitkeep
    │   ├── knowledge.md
    │   ├── knowledge-zh.md
    │   ├── skill.md
    │   └── skill-zh.md
    ├── prompts
    │   └── devin-resume-extract-prompt.md
    ├── README.md
    ├── scripts
    │   └── validate-taxonomy-refs.py
    ├── taxonomy-detail.md                  # prose descriptions for each T/L
    ├── taxonomy.yaml                       # skill taxonomy
    └── wiki.json                           # page schemas
    ```

**Step 2 — Invoke via Devin or DeepWiki Ask:**

```
@devin Read .devin/prompts/devin-resume-extract-prompt.md and execute all phases
against this repository. Load .devin/taxonomy.yaml as the authoritative
skill taxonomy. Output the four deliverable files into pages/.
```

**Step 3 — Post-generation validation:**

```bash
python .devin/scripts/validate-taxonomy-refs.py
```

The key design decisions in this prompt template are: the **phased execution pipeline** (scan → skill:en → skill:zh → knowledge:en → knowledge:zh) ensures each deliverable builds on the previous one's data; the **gated Phase 0** forces a complete codebase survey before any claims are made, preventing hallucinated skills; the **validation checklist** acts as a self-audit before output, catching mismatches between taxonomy codes and page content; and the **mirror constraint** on Chinese pages guarantees structural parity with the English source of truth while respecting the bilingual ATS keyword convention (`English / 中文释义`).